# 哈希表原理及应用

## 哈希表核心原理
+ 哈希表 + 数组 = ArrayHashMap、哈希表 + 双链表 = LinkedHashMap
+ **哈希表和Map不一样** -  根据底层数据结构不同，map的复杂度不一定都是O(1)
+ key为唯一，value可以重复
+ 哈希函数是把任意长度的非整数类型输入（key）→ 固定长度的整数输出（索引）
  + 这个函数的性能很关键，如果这个哈希函数复杂度是O(N)，那么哈希表的增删查改性能就会退化成O(N)
  + 这个函数还要保证的一点是，输入相同的key，输出也必须要相同，这样才能保证哈希表的正确性。
    > 示例 Java：全局唯一内存地址 负载因子：0.75
    > + int（[-2³¹, 2³¹ − 1]）可能为负数
    > + 用 % 来保证索引永远落在数组的合法范围
  
### 哈希冲突
0. 哈希桶 - 索引位置的存储单元
1. 纵向延伸 - 拉链法（Separate Chaining） O(k)为链表长度
2. 横向延伸 - 开放寻址法（Open Addressing） O(k)为连续探查的次数

+ 负载因子 
  + 哈希表装满的程度，= size / table.length
  + 哈希表内元素达到负载因子时，哈希表会自动扩容。

+ 哈希表中键的遍历顺序是无序的
  + key 在底层 table 数组中的分布是随机的
  + 在扩容的时候索引发生变化
  
+ key必须为不可变类型
  + 复杂度退化到O(n)
  + 丢失会导致内存泄漏

### 哈希表基本API
```
interface Map<K, V> {
    // 获取 key 对应的 value，时间复杂度 O(1)
    V get(K key);

    // 添加/修改 key-value 对，时间复杂度 O(1)
    void put(K key, V value);

    // 删除 key-value 对，时间复杂度 O(1)
    void remove(K key);

    // 是否包含 key，时间复杂度 O(1)
    boolean containsKey(K key);

    // 返回所有 key，时间复杂度 O(N)
    List<K> keys();
}
```

## 🔒链表加强哈希表（LinkedHashMap）
+ 哈希表的遍历顺序不可靠
  + Golang的哈希表遍历结果为随机（伪随机 - 头元素随机，环形遍历）
  + python 3.7+，哈希表dict的遍历顺序就是插入顺序（哈希链表）

### 哈希链表（LinkedHashMap）
**哈希表本质上就是一个键值映射，链表本质上就是一个顺序存储元素的容器。**
+ 通过哈希链表结构
  + 查 O(1)
  + 头尾插入 O(1)
  + 定节点删除 O(1)
  + 遍历顺序即插入顺序
  > 普通链表的删除是O(n)：因为需要先找到要删的节点
  > 哈希链表的删除是O(1)：给定双链表节点删除操作，因为哈希表可以直接给出节点位置

### 代码实现
``` python
class MyLinkedHashMap:
# 节点结构定义
    class Node:
        def __init__(self, key, val):
            self.key = key
            self.val = val
            self.next = None
            self.prev = None

# LinkedHashMap结构定义
    def __init__(self):
        self.head = self.Node(None, None)
        self.tail = self.Node(None, None)
        # 双向空链表构建，head <-> tail
        self.head.next = self.tail
        self.tail.prev = self.head
        self.map = dict()

# CRUD api（get, put, remove）
    def get(self, key):
        if key not in self.map:
            return None
        return self.map[key].val

    def put(self, key, val):
        # 若为新插入的节点，则同时插入链表和 map
        if key not in self.map:
            # 插入新的 Node
            node = self.Node(key, val)
            self.add_last_node(node)
            self.map[key] = node
            return
        # 若存在，则替换之前的 val
        self.map[key].val = val

    def remove(self, key):
        # 若 key 本不存在，直接返回
        if key not in self.map:
            return
        # 若 key 存在，则需要同时在哈希表和链表中删除
        node = self.map[key]
        del self.map[key]
        self.remove_node(node)

    def contains_key(self, key):
        return key in self.map

    def keys(self):
        key_list = []
        p = self.head.next
        while p != self.tail:
            key_list.append(p.key)
            p = p.next
        return key_list

    def add_last_node(self, x):
        temp = self.tail.prev
        # temp <-> tail

        x.next = self.tail
        x.prev = temp
        # temp <- x -> tail

        temp.next = x
        self.tail.prev = x
        # temp <-> x <-> tail

    def remove_node(self, x):
        prev = x.prev
        next = x.next
        # prev <-> x <-> next

        prev.next = next
        next.prev = prev

        x.next = x.prev = None


if __name__ == '__main__':
    map = MyLinkedHashMap()
    map.put("a", 1)
    map.put("b", 2)
    map.put("c", 3)
    map.put("d", 4)
    map.put("e", 5)

    print(map.keys())  # ['a', 'b', 'c', 'd', 'e']
    map.remove("c")
    print(map.keys())  # ['a', 'b', 'd', 'e']
```

## 🔒数组加强哈希表（ArrayHashMap）
### randomKey() API
+ 新增API：随机返回一个 key，要求时间复杂度 O(1) --- 数组实现
+ 直接使用一次或多次random()的弊端
  + 随机引用可能为空
  + 复杂度上升到O(n)
  + 拉链法中的随机不为均匀随机

### 实现思路
+ **在哈希表中建立数组元素到数组索引的映射关系**
+ 新建一个array来存储数据，hash来保持索引，即可以在O(1)的复杂度内完成随机取值
+ remove通过先将目标交换至array尾部再删除，即可以在O(1)内完成
+ 交换删除的方法会导致数组元素会被打乱

### 代码实现
```
// 用node hashmap arrayList共同实现
// node 存放 key value
// hashmap 存放 key index
// arrayList 存放 node
// 具体实现时需注意同时维护两个数据结构
```